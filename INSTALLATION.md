# 🚀 Инструкция по запуску MusicFinder

## Требования
- Windows 10/11
- Интернет соединение

## Установка

### 1. Установите Node.js
1. Скачайте с https://nodejs.org/en/download
2. Выберите "LTS" версию для Windows
3. Запустите установщик и следуйте инструкциям
4. После установки перезагрузите компьютер

### 2. Распакуйте проект
1. Распакуйте архив `MusicFinder-Project.zip`
2. Например в: `C:\Projects\Music-Finder`

### 3. Установите зависимости
1. Откройте папку проекта
2. В адресной строке проводника введите `powershell` и нажмите Enter
3. Выполните команду:
```
npm install
```
Подождите 1-2 минуты пока установятся все пакеты

### 4. Запустите сервер
В том же окне PowerShell выполните:
```
npm start
```

Вы должны увидеть:
```
🚀 MusicFinder server is running on http://localhost:3000
✅ MongoDB connected successfully
```

### 5. Откройте сайт
Откройте браузер (Chrome, Firefox, Edge) и перейдите по адресу:
```
http://localhost:3000
```
или
```
http://127.0.0.1:3000
```

## 🎵 Использование

1. **Регистрация**: Нажмите "Sign Up" и создайте аккаунт
2. **Поиск музыки**: Используйте строку поиска для поиска треков и артистов
3. **Плейлисты**: Создавайте свои плейлисты в разделе "My Playlists"
4. **Избранное**: Добавляйте любимые треки в "Favorites"

## 🔧 Возможные проблемы

### Ошибка "npm не является внутренней командой"
- Node.js не установлен или не добавлен в PATH
- Решение: Переустановите Node.js и перезагрузите компьютер

### Ошибка "Cannot find module"
- Не установлены зависимости
- Решение: Запустите `npm install` еще раз

### Ошибка "Port 3000 is already in use"
- Порт занят другим приложением
- Решение: Закройте другие приложения или измените порт в файле `.env`

### Не загружается музыка
- Проверьте интернет соединение
- Spotify API credentials должны быть корректными в файле `.env`

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте что Node.js установлен: `node --version`
2. Проверьте что npm работает: `npm --version`
3. Убедитесь что файл `.env` существует в корне проекта
4. Проверьте что все файлы проекта распакованы

## 🛑 Остановка сервера

Чтобы остановить сервер:
- Нажмите `Ctrl + C` в окне PowerShell где запущен сервер
- Или просто закройте окно PowerShell

---

**Приятного использования! 🎵**
